# Tóm tắt Khóa luận tốt nghiệp

## Đề tài: Xây dựng Pipeline xử lý dữ liệu luồng thời gian thực từ Binance & Phát hiện hành vi bất thường (Anomaly Detection)

Trong bối cảnh thị trường tiền mã hóa toàn cầu đang phát triển vượt bậc với biến động cực kỳ nhanh chóng, các sàn giao dịch lớn như Binance phải xử lý hàng triệu giao dịch mỗi ngày với tốc độ khớp lệnh lên đến hàng nghìn giao dịch mỗi giây. Dòng chảy dữ liệu tốc độ cao này ẩn chứa nhiều hành vi bất thường như thao túng thị trường (wash trading tạo khối lượng giao dịch ảo bằng bot), hay các giao dịch của các nhà đầu tư lớn (whale) gây trượt giá nghiêm trọng. Tuy nhiên, các phương pháp xử lý dữ liệu dạng lô (Batch processing) truyền thống thường có độ trễ lớn, không thể cung cấp thông tin kịp thời để giám sát dòng tiền hoặc đưa ra các cảnh báo rủi ro tức thì. Đồng thời, việc áp dụng kiến trúc Lambda truyền thống đòi hỏi phải duy trì song song hai luồng mã nguồn (Batch và Speed layers), dễ dẫn đến sự không nhất quán dữ liệu và tốn kém chi phí vận hành. Từ thực tiễn đó, đề tài khóa luận này được thực hiện nhằm xây dựng một hệ thống Data Pipeline xử lý dữ liệu luồng thời gian thực tối ưu, có khả năng thu thập, xử lý làm sạch, phát hiện hành vi bất thường tức thì và lưu trữ hiệu quả để hỗ trợ ra quyết định.

Để giải quyết bài toán trên, khóa luận đề xuất giải pháp xây dựng đường ống dữ liệu thời gian thực áp dụng kiến trúc Kappa thống nhất, sử dụng Apache Kafka làm lớp đệm thu nhận dữ liệu trực tiếp từ Binance WebSocket (nén LZ4 để tối ưu thông lượng) và Apache Spark Structured Streaming làm công cụ xử lý chính. Hệ thống thực hiện quy trình biến đổi dữ liệu 7 bước bao gồm ép kiểu, làm sạch, và cơ chế khử trùng lặp dữ liệu hai tầng (kết hợp Watermark 30 giây trong Spark và ràng buộc UPSERT tại cơ sở dữ liệu) nhằm đảm bảo tính toàn vẹn và duy nhất của dữ liệu (exactly-once semantics). Điểm cốt lõi của bộ xử lý là việc tích hợp các quy tắc thống kê động dựa trên cửa sổ trượt thời gian thực (Z-Score cho các giao dịch khối lượng đột biến, Wash Trade Cluster cho các cụm giao dịch trùng lặp mili-giây, và Slippage cho biến động trượt giá). Dữ liệu sau khi xử lý được ghi nhận song song xuống hệ thống lưu trữ kép (Dual-Sink) gồm PostgreSQL (ghi đồng bộ) và Google BigQuery (ghi bất đồng bộ qua tệp Parquet kèm cơ chế Dead-Letter Queue để xử lý lỗi mạng) theo mô hình Star Schema chuẩn Kimball, làm cơ sở dữ liệu cho lớp trực quan hóa động trên Power BI.

Kết quả thử nghiệm thực tế với luồng dữ liệu của 5 cặp giao dịch lớn (BTC, ETH, BNB, SOL, XRP) cho thấy hệ thống vận hành ổn định với độ trễ (latency) xử lý và lưu trữ cực thấp (dưới 500 mili-giây). Hệ thống cũng chứng minh khả năng chịu lỗi và tự phục hồi mạnh mẽ nhờ cơ chế Checkpointing và ghi bù dữ liệu từ Dead-Letter Queue mà không làm thất thoát thông tin. Đây là một giải pháp ứng dụng thực tiễn hoàn chỉnh, không chỉ giải quyết trọn vẹn bài toán kỹ thuật từ khâu thu thập đến lưu trữ dữ liệu lớn theo thời gian thực mà còn cung cấp một công cụ trực quan hóa trực quan, giúp người dùng và các nhà quản lý giám sát hiệu quả dòng tiền thị trường để đưa ra quyết định kịp thời.
